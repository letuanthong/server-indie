# ============================================================
# server-indie - Environment Configuration
# Copy file này thành .env và chỉnh sửa cho phù hợp:
#   cp .env.example .env
#   nano .env
# ============================================================

# === DATABASE ===
DB_NAME=whis_1
DB_NAME_DATA=whis_2
DB_USER=dragonserver
DB_PASS=DragonServer@2025

# === SERVER ===
SERVER_PORT=14445
GAME_USER=gameserver

# === JVM MEMORY ===
# Điều chỉnh theo RAM server thật:
#   2GB RAM  → JAVA_XMS=512m  JAVA_XMX=1g
#   4GB RAM  → JAVA_XMS=1g    JAVA_XMX=2g   (mặc định)
#   8GB RAM  → JAVA_XMS=2g    JAVA_XMX=4g
#   16GB RAM → JAVA_XMS=4g    JAVA_XMX=8g
JAVA_XMS=512m
JAVA_XMX=1g

# === BACKUP ===
BACKUP_DIR=/home/gameserver/backups
KEEP_DAYS=7

# === SOURCE CODE ===
# Nếu server chạy ở user khác (vd: gameserver) mà git repo ở root,
# chỉ định đường dẫn source để rebuild.sh tự sync code:
#SOURCE_DIR=/root/server-indie
